#include <bits/stdc++.h>

using namespace std;
int main()
{
    int t; cin>>t;
    while(t--)
    {
        int n; cin>>n;
        int a[n];
        bool arr[1000001];
        memset(arr, false, sizeof(arr));
        for(int &x:a) cin>>x;
        for(int x:a)
        arr[x] = true;
        for(int i = 0 ; i<1000001; i++){
            if(arr[i])
            cout<<"YES";
        }
        
    }
}
