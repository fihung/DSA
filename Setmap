#include <bits/stdc++.h>

using namespace std;
int main()
{
    int t; cin>>t;
    while(t--)
    {
        int n; cin>>n;
        set<int> arr;
        for(int i = 0 ; i<n ; i++){
            int x; cin>>x;
            arr.insert(x);
        }
       int q; cin>>q;
       while(q--)
       {
        int inspect; cin>>inspect;
        if(arr.find(inspect) != arr.end() )
        cout<<"True";
        else
        cout<<"False";
       }
    }
}
