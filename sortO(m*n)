#include <iostream>

using namespace std;
void inPut(int a[], int n)
{
	for(int i = 0;  i<n; i++)
	cin>>a[i];
}
void swap(int &a, int &b)
{
	int temp;
	temp  = a;
	a = b;
	b = temp;
}
int main()
{
	int a[100];
	int n; cin>>n;
	inPut(a,n);
	for (int i = 0; i < n - 1; i++) {
		for (int j = i+1; j < n; j++) {
			 if( a[i] > a[j])
	            swap(a[i], a[j]);
		}
	}
	for(int i = 0; i <n; i++)
	    cout<<a[i]<<" ";
}
