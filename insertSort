#include <iostream>

using namespace std;

void inPut(int a[], int n)
{
	for (int i = 0 ; i<n; i++)
	cin>>a[i];
}

int main()
{
	int a[100];
	int n; cin>>n;
	int j = 0; 
	inPut(a,n);
	for (int j = 1; j<n; j++) {
		int i = j - 1
		int key = a[j];
		while (key < a[i] && i>=0) {
			a[i+1] = a[i];
			i--;
		}
		a[i+1] = key;

	}
	
